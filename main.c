#include <stdio.h>
#include <stdlib.h>

isfgasga
int main(int argc, char const *argv[]) {
  FILE* in,out;
  in = fopen(,"r");
  out = fopen(,"w");
  fprintf(out, "%s\n", );
  teseeeee
  return 0;
}
